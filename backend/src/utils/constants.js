/**
 * Application Constants
 */

const AI_ROLES = {
  SYSTEM: 'system',
  USER: 'user',
  ASSISTANT: 'assistant'
};

const DOCUMENT_STATUS = {
  PENDING: 'pending',
  PROCESSING: 'processing',
  READY: 'ready',
  FAILED: 'failed'
};

module.exports = {
  AI_ROLES,
  DOCUMENT_STATUS
};
